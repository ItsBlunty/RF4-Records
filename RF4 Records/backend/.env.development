# Local PostgreSQL Development Database
PGDATABASE=rf4_records_dev
PGUSER=postgres
PGPASSWORD=localpassword
PGHOST=localhost
PGPORT=5432

# Docker container defaults (if using Docker)
POSTGRES_PASSWORD=${PGPASSWORD}
POSTGRES_DB=${PGDATABASE}

# Admin Authentication Token (for testing admin endpoints)
ADMIN_TOKEN=rf4-admin-2025-a8b9c2d3e4f5g6h7i431dds1m2n3o4p5